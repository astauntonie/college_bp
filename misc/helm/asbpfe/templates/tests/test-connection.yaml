apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "asbpfe.fullname" . }}-test-connection"
  labels:
    {{- include "asbpfe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "asbpfe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
